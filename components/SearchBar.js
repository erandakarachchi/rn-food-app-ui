import React from "react";
import { View, Text } from "react-native";

const SearchBar = () => {
  return (
    <View>
      <Text>Find Recipes or chef</Text>
    </View>
  );
};

export default SearchBar;
